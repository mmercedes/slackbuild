{
    "attachments": [
        {
            "title": "Build in ${project_id}",
            "text": "*${repo_name}*  <${revision_url}|${revision_sha_short}>\n${build_status} | <${build_log_url}|Logs>",
            "fallback": "${build_status} | <${build_log_url}|Logs>",
            "color": "${build_color}",
            "footer": "ID: ${build_id_short} $build_duration",
            "mrkdwn_in": ["text"]
        }
    ],
    "channel": "${channel}"
}
