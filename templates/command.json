{
    "attachments": [
        {
            "text": "${result}",
            "fallback": "${result}",
            "mrkdwn_in": ["text"],
            "color": "${color}"
        }
    ],
    "channel": "${channel}"
}
